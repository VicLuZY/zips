# zips

## Frontend

A lightweight map UI is available in `frontend/` with:

- Leaflet map view
- Viewport (`bbox`) based `/restaurants` fetching with debounce
- "No tips only" filter enabled by default
- Marker/list synchronization and detail card interactions
